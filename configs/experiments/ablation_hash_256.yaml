# configs/experiments/ablation_hash_256.yaml
# AB-4: Hash Bits = 256 消融實驗

defaults:
  - cv_experiment

model:
  hash:
    bits: 256

experiment:
  name: "ablation_hash_256"
  tags: ["ablation", "hash-bits", "hash-256"]

training:
  num_epochs: 50
  early_stopping_patience: 5
